def podoju(idx, li):
    global podo
    if idx == N:
        if podo < sum(li):
            podo = sum(li)
    else:
        for i in range()

N = int(input())
arr = [int(input()) for _ in range(N)]
podo = 0